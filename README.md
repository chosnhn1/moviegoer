# Moviegoer: Movie-related CRUD app project, rewritten with Spring Boot & React

## Architecture

* Backend: REST API provided with Spring MVC
  * User: CRUD articles refs with Movie Entity
  * Endpoint:
    * api/v1/
    * auth: handle JWT auth
    * articles: handle article CRUD
    * accounts: handle User info


